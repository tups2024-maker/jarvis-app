const meta={dashboard:['JARVIS V4','実績・売上・欠車を統合した経営ダッシュボード'],delivery:['配送・シフト','案件別の運行・配置管理'],absence:['欠車対応','欠車・不足案件の確認'],sales:['売上・利益','案件別売上・欠車損失'],locations:['案件別実績','案件単位の稼働状況'],ai:['AI分析','JARVISによる経営判断'],settings:['設定','単価・データ連携・システム']};function go(page){document.querySelectorAll('.page').forEach(x=>x.classList.remove('active'));document.querySelectorAll('.nav-item').forEach(x=>x.classList.remove('active'));document.getElementById('page-'+page)?.classList.add('active');document.querySelector(`[data-page="${page}"]`)?.classList.add('active');document.getElementById('pageTitle').textContent=meta[page][0];document.getElementById('pageSub').textContent=meta[page][1];closeMenu()}document.querySelectorAll('[data-page]').forEach(b=>b.addEventListener('click',()=>go(b.dataset.page)));const menu=document.getElementById('sideMenu'),overlay=document.getElementById('overlay');function openMenu(){menu?.classList.add('open');overlay?.classList.add('show')}function closeMenu(){menu?.classList.remove('open');overlay?.classList.remove('show')}document.getElementById('openMenu')?.addEventListener('click',openMenu);document.getElementById('closeMenu')?.addEventListener('click',closeMenu);overlay?.addEventListener('click',closeMenu);const yen=n=>'¥'+Math.round(n).toLocaleString('ja-JP');async function load(){const r=await fetch('./jarvis-v4-data.json?ts='+Date.now(),{cache:'no-store'});const d=await r.json();const active=d.projects.reduce((s,p)=>s+p.active,0),abs=d.projects.reduce((s,p)=>s+p.absence,0);const missing=d.projects.filter(p=>!p.unit_price);const monthSales=d.projects.reduce((s,p)=>s+((d.month_actuals[p.name]||0)*(p.unit_price||0)),0);const todaySales=d.projects.reduce((s,p)=>s+(p.active*(p.unit_price||0)),0);const loss=d.projects.reduce((s,p)=>s+(p.absence*(p.unit_price||0)),0);document.getElementById('kpiActive').textContent=active+' 台';document.getElementById('kpiAbsence').textContent=abs+' 台';document.getElementById('kpiSales').textContent=yen(monthSales);document.getElementById('kpiLoss').textContent=yen(loss);document.getElementById('salesMonth').textContent=yen(monthSales);document.getElementById('salesToday').textContent=yen(todaySales);document.getElementById('salesLoss').textContent=yen(loss);document.getElementById('rateCoverage').textContent=Math.round((d.projects.length-missing.length)/d.projects.length*100)+'%';document.getElementById('absenceCount').textContent=abs+' 件';document.getElementById('projectStatus').innerHTML=d.projects.map(p=>`<div class="project-card ${p.active<p.required?'warn':'ok'}"><b>${p.name}</b><span>${p.active} / ${p.required} 台</span><small>${p.active<p.required?'不足 '+(p.required-p.active)+' 台':'充足'}</small></div>`).join('');document.getElementById('missingRates').innerHTML=missing.map(p=>`<div>${p.name}：単価未設定</div>`).join('')||'<div>すべて設定済み</div>';document.getElementById('shortageList').innerHTML=d.projects.filter(p=>p.active<p.required).map(p=>`<div>${p.name}：不足 ${p.required-p.active} 台</div>`).join('')||'<div>不足なし</div>';const head='<div class="thead"><span>案件</span><span>必要</span><span>稼働</span><span>差異</span><span>単価</span></div>';document.getElementById('deliveryTable').innerHTML=head+d.projects.map(p=>`<div><span>${p.name}</span><span>${p.required}</span><span>${p.active}</span><span>${p.active-p.required}</span><span>${p.unit_price?yen(p.unit_price):'未設定'}</span></div>`).join('');document.getElementById('salesTable').innerHTML=head+d.projects.map(p=>`<div><span>${p.name}</span><span>${d.month_actuals[p.name]||0}</span><span>${p.unit_price?yen(p.unit_price):'未設定'}</span><span>${p.unit_price?yen((d.month_actuals[p.name]||0)*p.unit_price):'—'}</span><span>${p.absence?yen(p.absence*(p.unit_price||0)):'¥0'}</span></div>`).join('');document.getElementById('projectCards').innerHTML=d.projects.map(p=>`<article class="glass location"><b>${p.name}</b><span>${p.active} / ${p.required} 台</span><small>${p.unit_price?yen(p.unit_price):'単価未設定'}</small></article>`).join('');document.getElementById('aiSummary').textContent=`本日は稼働 ${active} 台、欠車 ${abs} 件。${d.projects.filter(p=>p.active<p.required).length}案件で不足があります。`;};load().catch(console.error);if('serviceWorker'in navigator){navigator.serviceWorker.register('sw.js');}